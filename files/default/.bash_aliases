# Keep compatible with zsh and bash (fallback) - no functions and stuff!

alias h='history'
alias j="jobs -l"
alias j="jobs -l"

#open any file or folder with default app
alias o='xdg-open '
alias hf='history | grep $1′

# Disk free space
alias df='df -Th' 

# Disk usage
alias du='du -h' 